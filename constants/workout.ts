/** Starting values for a set when there's no previous set to duplicate. */
export const DEFAULT_SET_VALUES = { weight: 45, reps: 10 };

/** How many sets a newly added exercise starts with. */
export const DEFAULT_SETS_PER_EXERCISE = 3;
