/** Height of the floating tab bar pill. */
export const TAB_BAR_HEIGHT = 64;

/** Gap between the tab bar and the screen edges / safe area. */
export const TAB_BAR_MARGIN = 16;

/** Gap kept between scrolling content and the top edge of the tab bar. */
export const TAB_BAR_CONTENT_GAP = 8;
