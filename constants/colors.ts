// Raw token values for the few places className can't reach
// (vector icon colors, navigation container styles).
// Keep in sync with tailwind.config.js.
export const colors = {
  background: "#131313",
  surface: "#1E1E1E",
  surfaceRaised: "#242424",
  border: "#2A2A2A",
  accent: "#E8590C",
  textPrimary: "#E5E2E1",
  textSecondary: "rgba(255,255,255,0.6)",
} as const;
